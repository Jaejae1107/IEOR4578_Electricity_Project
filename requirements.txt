pandas>=2.0
numpy>=1.24
pyarrow>=12.0
scikit-learn>=1.3
matplotlib>=3.8

# Modeling — Step 1 (AutoARIMA, AutoETS)
statsforecast>=2.0
joblib>=1.3

# Modeling — Step 2 (SARIMAX, Prophet)
prophet>=1.1

# Modeling — Step 3 / Step 6 (iTransformer)
neuralforecast>=1.7
